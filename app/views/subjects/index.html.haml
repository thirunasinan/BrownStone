.container
  = search_form_for @sources_q do |f|
    .form-group
      = f.label :name_cont_or_subject_name_cont, "Search Sources by Name: "
      = f.search_field :name_cont, class: 'form-control'

    = f.submit class: 'btn btn-default'

  %br
  %br
  %br

  %h4 Subjects :
  %br
  = link_to "add new", rails_admin.new_path(model_name: "Subject")
  %br
  %br
  %br

  %table.table.table-hover.sources-table
    %tr
      %th.subject
        = Subject

    - @subjects.each do |subject|
      %tr
        %td
          = link_to subject.name, "/sources_by_subject/#{subject.id}"

  =# will_paginate @subjects, renderer: BootstrapPagination::Rails
