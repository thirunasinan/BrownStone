Tag explorer:

“Add new tag” button
Add new tag/add new group - fields should match same fields in tag manager
allow them to add description (of tag) if creating new tag

how this will work with state :

update tag within tagExplorerTagRelationship
