Tag explorer:

“Add new tag” button
Clears modal so focus is just on making a new tag (with subject / tag group drop-downs / button to create new tag group)
Add new tag/add new group - fields should match same fields in tag manager
allow them to add description if creating new tag
